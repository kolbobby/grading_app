<% if !user.teacher? %>
	<tr id="<%= user.id %>">
		<td class="u_name"><%= link_to user.name, user_path %></td>
		<td class="u_period"><%= user.period %></td>
		<td class="u_grade_level"><%= user.grade_level %></td>
	</tr>
<% end %>
