<% provide(:title, @user.name) %>
<div class="col pull-left tabular">
	<table>
		<thead>
			<tr>
				<td></td>
				<td><h3><%= @user.name %></h3></td>
			</tr>
		</thead>
		<tbody>
			<% if @user.u_type = 1 %>
				<%= render 'student_side' %>
			<% else %>
			<tr>
				<td>asdf</td>
			</tr>
			<% end %>
		</tbody>
	</table>
</div>